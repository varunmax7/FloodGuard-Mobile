/// Non-web stub. Every method is a no-op so the calling code can stay simple.
library;

Future<bool> requestPermission() async => false;
void show({
  required String title,
  required String body,
  String? image,
  String? tag,
}) {}
